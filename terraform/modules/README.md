# Shared Terraform modules

Provider-agnostic building blocks used by `terraform/aws`, `terraform/azure`,
and `terraform/local`. Modules here should not reference a specific cloud
provider's resource types.
